// stand.resize(number1); // 初始化 stand 的大小
    // for(int i = 0; i < number1; i++){
    //     char path[50];
    //     sprintf(path, "./hero/stand/frame_%d.png", i);
    //     stand.push_back(path);
    //     //printf("Loading: %s\n", path); // 在构造时打印路径
    // }

    // walk.resize(number2); // 初始化 walk 的大小
    // for (int i = 0; i < number2; ++i) {
    //     char path[50];
    //     sprintf(path, "./hero/walk/frame_%d.png", i);
    //     walk.push_back(path);
    //     //printf("Loading: %s\n", path); // 在构造时打印路径
    // }

    // attack.resize(number3); // 初始化 attack 的大小
    // for (int i = 0; i < number3; ++i) {
    //     char path[50];
    //     sprintf(path, "./hero/attack/frame_%d.png", i);
    //     attack.push_back(path);
    //     //printf("Loading: %s\n", path); // 在构造时打印路径
    // }

 //while(1){
     //    // cleardevice();
     //    // putimage(0, 0, &world);
     //    for(int i = 0; i < hero1.stand1.size(); i++){
     //        BeginBatchDraw();
     //        putimage(0, 0, &world);
     //        putimagePNG2(100, 180, &hero1.stand1[i]);
     //        EndBatchDraw();
     //        Sleep(200);
     //        cleardevice();
     //    }
     //}


//     MessageBox - 这是 Windows 用来显示简单对话框的函数
                    //     GetHWnd() - 获取当前窗口的句柄(通常指父窗口)
                    //    _T("This is a speedrun-themed menu demo.") - 要显示的消息内容，提到这是一个"速通主题的菜单演示"
                    //    _T("Intro") - 对话框的标题，显示为"Intro"
                    //    MB_OK - 表示对话框只包含一个"确定"按钮
                    /*MessageBox(GetHWnd(), _T("Options Menu is need update..."), _T("Info"), MB_OK);*/